{
  "name": "server",
  "version": "1.0.0",
  "description": "The server for the sw-redux example",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "mkdir -p dist",
    "build": "babel src --out-dir dist",
    "start": "npm run build; cd dist; node index.js"
  },
  "author": "Duncan Regan",
  "license": "BSD-2-Clause",
  "dependencies": {
    "body-parser": "^1.15.0",
    "cors": "^2.7.1",
    "express": "^4.13.3",
    "node-fetch": "^1.5.1",
    "sequelize": "^3.21.0",
    "shortid": "^2.2.6",
    "sqlite3": "^3.1.3"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0"
  }
}
